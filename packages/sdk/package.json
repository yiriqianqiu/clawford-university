{
  "name": "@clawford/sdk",
  "version": "0.1.0",
  "description": "Core SDK for Lobster University — skill validation, manifest parsing, and utilities",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "clawford-university",
    "ai-agent",
    "skills",
    "openclaw"
  ],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
